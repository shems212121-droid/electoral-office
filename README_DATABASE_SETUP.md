# 🎯 دليل سريع: إعداد قاعدة البيانات على Railway

## ✨ الطريقة السريعة (موصى بها - 5 دقائق)

### الخطوة 1: إعادة النشر على Railway
1. افتح https://railway.app/project/07f18b5f-995e-4875-b18f-992e801b4298
2. اضغط على خدمة "web"
3. اذهب إلى "Deployments"
4. اضغط على الـ 3 نقاط بجانب آخر نشر
5. اختر "Redeploy"
6. انتظر حتى ينتهي النشر (2-3 دقائق)

### الخطوة 2: تسجيل الدخول
1. افتح https://web-production-42c39.up.railway.app
2. سجل دخول بـ:
   - **المستخدم**: `admin`
   - **كلمة المرور**: `admin123`

### الخطوة 3: غيّر كلمة المرور!
- اذهب للإعدادات وغيّر كلمة مرور admin فوراً

**✅ انتهيت! الموقع جاهز للاستخدام**

---

## 📦 الطريقة الكاملة (إذا كان لديك بيانات من المشروع المحلي)

###  1️⃣ تصدير البيانات المحلية

```powershell
cd C:\Users\2025\.gemini\antigravity\scratch\electoral_office
python export_data.py
```

ستحصل على ملفات:
- `users_data.json` - المستخدمون
- `candidates_data.json` - المرشحون
- `voters_data.json` - الناخبون
- ... إلخ

### 2️⃣ رفع البيانات للمستودع

```powershell
git add *_data.json
git add export_data.py import_data.py setup_production_db.py
git commit -m "إضافة البيانات الأولية وسكريبتات الإعداد"
git push origin main
```

### 3️⃣ تثبيت Railway CLI (مرة واحدة فقط)

```powershell
# في PowerShell كمسؤول:
iwr https://railway.app/install.ps1 -useb | iex
```

### 4️⃣ تسجيل الدخول وربط المشروع

```powershell
railway login
railway link
# اختر: valiant-presence
```

### 5️⃣ استيراد البيانات إلى الإنتاج

```powershell
railway run python import_data.py
```

**✅ تم! البيانات الآن على الإنتاج**

---

## 🔍 التحقق من نجاح الإعداد

✅ زر https://web-production-42c39.up.railway.app  
✅ سجل دخول بـ admin / admin123  
✅ تحقق من:
- لوحة التحكم تعرض إحصائيات
- صفحة المرشحين تعمل
- صفحة الناخبين تعمل
- البحث عن ناخب يعمل (اكتب رقم واضغط Enter)

---

## 🐛 حل المشاكل

### مشكلة: الملفات الثابتة (CSS) لا تعمل
```powershell
# على Railway Dashboard:
# Variables → أضف:
DISABLE_COLLECTSTATIC=0
# ثم Redeploy
```

### مشكلة: خطأ في قاعدة البيانات
```powershell
# تحقق من المتغيرات على Railway:
DATABASE_URL=${{Postgres-ky75.DATABASE_URL}}
# يجب أن يكون مرجع ديناميكي وليس قيمة ثابتة
```

### مشكلة: "command not found: railway"
```powershell
# أعد تشغيل PowerShell بعد التثبيت
# أو أعد تشغيل الكمبيوتر
```

---

## 📝 ملفات السكريبتات المتوفرة

| الملف | الوصف | متى تستخدمه |
|------|------|-------------|
| `setup_production_db.py` | إعداد شامل لقاعدة البيانات | عند الإعداد الأول |
| `export_data.py` | تصدير من SQLite المحلي | قبل رفع البيانات |
| `import_data.py` | استيراد إلى PostgreSQL | على Railway |

---

## ⚡ أوامر مفيدة

```powershell
# عرض سجلات الموقع الحية
railway logs

# تشغيل أمر على الخادم
railway run python manage.py <command>

# فتح shell على الخادم
railway run python manage.py shell

# عرض المتغيرات
railway variables

# فتح قاعدة البيانات
railway run python manage.py dbshell
```

---

## 🎉 ماذا بعد؟

بعد إعداد قاعدة البيانات:

1. ✅ غيّر كلمات مرور جميع المستخدمين
2. ✅ فعّل النسخ الاحتياطي التلقائي على Railway
3. ✅ أضف domain مخصص (اختياري)
4. ✅ فعّل HTTPS (تلقائي على Railway)
5. ✅ اختبر جميع الوظائف
6. ✅ ابدأ الاستخدام الفعلي! 🚀

---

**تم الإنشاء**: 2026-01-12  
**الموقع**: https://web-production-42c39.up.railway.app  
**Railway**: https://railway.com/project/07f18b5f-995e-4875-b18f-992e801b4298
