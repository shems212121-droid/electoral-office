# دليل إعداد قاعدة البيانات الإنتاجية على Railway

## الوضع الحالي
✅ تم حل مشكلة النشر  
✅ تم حذف قاعدة البيانات القديمة الممتلئة  
⚠️ قاعدة البيانات الجديدة (Postgres-ky75) فارغة وتحتاج إعداد  

---

## الطريقة الأولى: ✨ الإعداد التلقائي (الأسهل)

### الخطوات:
1. افتح Railway Dashboard على المتصفح
2. اذهب إلى مشروع **"valiant-presence"**
3. اضغط على خدمة **"web"**
4. اذهب إلى تبويب **"Deployments"**
5. اضغط على أيقونة الـ 3 نقاط بجانب آخر نشر
6. اختر **"Redeploy"**

### ماذا سيحدث:
- سيشتغل النشر من جديد
- سيعمل `migrate --noinput` تلقائياً (موجود في railway.json)
- سيعمل `python manage.py create_admin` تلقائياً
- الموقع سيكون جاهزاً!

### بيانات تسجيل الدخول بعد النشر:
- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin123`
- ⚠️ **مهم جداً**: غيّر كلمة المرور فوراً بعد تسجيل الدخول!

---

## الطريقة الثانية: 🔧 تشغيل السكريبت المحلي

إذا لم تنجح الطريقة الأولى، يمكنك:

### 1. تثبيت Railway CLI
```powershell
# في PowerShell كمسؤول:
iwr https://railway.app/install.ps1 -useb | iex
```

### 2. تسجيل الدخول
```powershell
railway login
```

### 3. ربط المشروع
```powershell
cd C:\Users\2025\.gemini\antigravity\scratch\electoral_office
railway link
# اختر: valiant-presence
```

### 4. تشغيل السكريبت
```powershell
railway run python setup_production_db.py
```

---

## الطريقة الثالثة: 📦 استيراد البيانات من المشروع المحلي

إذا كان لديك بيانات مهمة في `db.sqlite3`:

### 1. تصدير البيانات من SQLite
```powershell
python manage.py dumpdata --natural-foreign --natural-primary -e contenttypes -e auth.Permission > data_backup.json
```

### 2. رفع الملف للمشروع
```powershell
git add data_backup.json
git commit -m "إضافة نسخة احتياطية من البيانات"
git push origin main
```

### 3. تشغيل الاستيراد على Railway
```powershell
railway run python manage.py migrate
railway run python manage.py loaddata data_backup.json
```

---

## ✅ التحقق من نجاح الإعداد

بعد تنفيذ أي من الطرق، تحقق من:

1. **زيارة الموقع**: https://web-production-42c39.up.railway.app
2. **تسجيل الدخول** بـ: `admin` / `admin123`
3. **التحقق من الصفحات**:
   - لوحة التحكم تعمل ✅
   - صفحة المرشحين تعمل ✅
   - صفحة الناخبين تعمل ✅
   - البحث عن ناخب بالرقم يعمل ✅

---

## 🐛 حل المشاكل الشائعة

### المشكلة: "No module named 'psycopg2'"
**الحل**: تأكد من وجود `psycopg2-binary` في `requirements.txt`

### المشكلة: "Database connection failed"
**الحل**: تأكد من أن `DATABASE_URL` يشير لـ `${{Postgres-ky75.DATABASE_URL}}`

### المشكلة: "Static files not found" (ملفات CSS لا تعمل)
**الحل**: تأكد من تشغيل `collectstatic` في buildCommand

---

## 📝 ملاحظات مهمة

1. **البيانات القديمة**: قاعدة البيانات القديمة تم حذفها، إذا كانت لديك بيانات مهمة استخدم الطريقة 3
2. **كلمة المرور**: غيّر كلمة مرور admin فوراً بعد أول تسجيل دخول
3. **النسخ الاحتياطية**: فعّل النسخ الاحتياطي التلقائي على Railway:
   - Project → Database → Settings → Enable automated backups

---

## 🎯 الخطوات التالية بعد الإعداد

1. ✅ تغيير كلمة مرور admin
2. ✅ إنشاء المستخدمين الآخرين
3. ✅ استيراد بيانات المرشحين (إذا موجودة)
4. ✅ استيراد بيانات الناخبين (إذا موجودة)
5. ✅ اختبار جميع الوظائف
6. ✅ تفعيل النسخ الاحتياطي التلقائي

---

**آخر تحديث**: 2026-01-12 05:54
**رابط الموقع**: https://web-production-42c39.up.railway.app
**رابط Railway**: https://railway.com/project/07f18b5f-995e-4875-b18f-992e801b4298
