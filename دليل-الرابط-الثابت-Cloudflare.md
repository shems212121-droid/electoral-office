# دليل الإعداد: الرابط الثابت عبر Cloudflare

## 🎯 الهدف
الحصول على رابط ثابت لا يتغير أبداً (مثل: `electoral-basra.trycloudflare.com`)

---

## ⏱️ الوقت المطلوب
- **الإعداد الأولي:** 10 دقائق (مرة واحدة فقط)
- **الاستخدام اليومي:** 10 ثواني (شغّل ملف واحد)

---

## 📋 الخطوات

### المرحلة 1: إنشاء حساب Cloudflare (مرة واحدة - 3 دقائق)

1. **افتح المتصفح** واذهب إلى:
   ```
   https://dash.cloudflare.com/sign-up
   ```

2. **أدخل بياناتك:**
   - البريد الإلكتروني
   - كلمة مرور قوية
   - اضغط "Create Account"

3. **افتح بريدك الإلكتروني** وأكد الحساب

4. **سجل الدخول** إلى لوحة التحكم

---

### المرحلة 2: إنشاء Tunnel (مرة واحدة - 7 دقائق)

#### الطريقة الأوتوماتيكية (سهلة):

1. **شغّل الملف:** `7-إنشاء-رابط-ثابت-Cloudflare.bat`

2. **اتبع التعليمات** على الشاشة:
   - سيفتح المتصفح تلقائياً
   - سجل الدخول إلى Cloudflare
   - اضغط "Authorize" عندما يطلب
   - انتظر حتى يكتمل الإعداد

3. **احفظ الرابط** الذي سيظهر لك!

#### الطريقة اليدوية (إذا لم تنجح الأوتوماتيكية):

1. **افتح Terminal** (cmd) في مجلد البرنامج

2. **سجل الدخول:**
   ```
   cloudflared tunnel login
   ```
   - سيفتح المتصفح
   - اختر حسابك
   - اضغط "Authorize"

3. **أنشئ Tunnel:**
   ```
   cloudflared tunnel create electoral-office
   ```
   - احفظ الـ UUID الذي سيظهر

4. **أنشئ ملف الإعدادات:**
   - افتح Notepad
   - انسخ هذا المحتوى:
   ```yaml
   tunnel: electoral-office
   credentials-file: C:\Users\[اسمك]\.cloudflared\[UUID].json

   ingress:
     - hostname: electoral-basra.trycloudflare.com
       service: http://localhost:8000
     - service: http_status:404
   ```
   - استبدل `[اسمك]` و `[UUID]` بالقيم الصحيحة
   - احفظ الملف باسم: `config.yml` في مجلد `.cloudflared`

5. **اربط الـ DNS:**
   ```
   cloudflared tunnel route dns electoral-office electoral-basra.trycloudflare.com
   ```

---

### المرحلة 3: الاستخدام اليومي (10 ثواني)

**كل يوم، فقط:**
1. شغّل الملف: `8-تشغيل-رابط-ثابت.bat`
2. ✅ جاهز! شارك الرابط مع الموظفين

---

## 🌐 الرابط النهائي

بعد الإعداد، الرابط الثابت سيكون:
```
https://electoral-basra.trycloudflare.com
```

**شارك هذا الرابط مع:**
- المرشحين
- الدعم الفني
- غرف العمليات
- أي جهاز يحتاج الوصول

---

## 📱 كيفية الوصول من الهاتف

1. **افتح المتصفح** في الهاتف
2. **اكتب الرابط:** `https://electoral-basra.trycloudflare.com`
3. **سجل الدخول** باسم المستخدم وكلمة المرور
4. ✅ جاهز!

**لا حاجة لتثبيت أي تطبيقات!**

---

## 🔐 الأمان

- ✅ HTTPS تلقائي (اتصال مشفر)
- ✅ يعمل فقط مع Cloudflare (موثوق عالمياً)
- ✅ لا يمكن الدخول إلا بحساب مسجل في النظام

---

## 🆘 حل المشاكل

### المشكلة: "cloudflared: command not found"
**الحل:**
- تأكد من وجود ملف `cloudflared.exe` في مجلد البرنامج
- إذا لم يكن موجوداً، سيتم تحميله تلقائياً

### المشكلة: الرابط لا يعمل
**الحل:**
1. تأكد من تشغيل `8-تشغيل-رابط-ثابت.bat`
2. تأكد من أن Django يعمل (النافذة الأولى)
3. انتظر 10 ثواني بعد التشغيل

### المشكلة: "Tunnel credentials not found"
**الحل:**
- أعد تشغيل `7-إنشاء-رابط-ثابت-Cloudflare.bat`
- أو اتبع الطريقة اليدوية أعلاه

---

## ✅ الخلاصة

| المرحلة | الوقت | متى؟ |
|---------|-------|------|
| إنشاء حساب Cloudflare | 3 دقائق | مرة واحدة |
| إعداد Tunnel | 7 دقائق | مرة واحدة |
| الاستخدام اليومي | 10 ثواني | كل يوم |

**النتيجة:** رابط ثابت يعمل من أي جهاز دون تثبيت تطبيقات!
