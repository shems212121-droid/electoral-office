---
description: تشغيل المكتب الانتخابي للوصول من الإنترنت عبر ngrok
---

# تشغيل المكتب الانتخابي للوصول العام من الإنترنت

هذا الدليل يشرح كيفية تشغيل التطبيق بحيث يمكن الوصول إليه من أي جهاز على أي شبكة إنترنت.

## الخطوة 1: التحضيرات (مرة واحدة فقط)

### 1.1 تحميل ngrok

1. افتح المتصفح واذهب إلى: https://ngrok.com/download
2. حمل النسخة المناسبة لنظام Windows
3. فك ضغط الملف المحمل (ngrok.zip)
4. ضع ملف `ngrok.exe` في مجلد سهل الوصول، مثل:
   ```
   C:\ngrok\ngrok.exe
   ```

### 1.2 إنشاء حساب ngrok (اختياري ولكن موصى به)

1. اذهب إلى: https://dashboard.ngrok.com/signup
2. سجل حساب مجاني
3. بعد تسجيل الدخول، انسخ الـ authtoken من: https://dashboard.ngrok.com/get-started/your-authtoken
4. في PowerShell، نفذ الأمر التالي (استبدل YOUR_AUTHTOKEN بالرمز الخاص بك):
   ```powershell
   C:\ngrok\ngrok.exe config add-authtoken YOUR_AUTHTOKEN
   ```

> **ملاحظة:** بدون authtoken، ستحصل على جلسة مؤقتة (8 ساعات فقط). مع authtoken، ستحصل على ميزات إضافية.

---

## الخطوة 2: تشغيل التطبيق

### 2.1 تشغيل خادم Django

// turbo
في PowerShell الأول، نفذ:
```powershell
cd C:\Users\2025\.gemini\antigravity\scratch\electoral_office
python manage.py runserver 0.0.0.0:8000
```

> **مهم:** استخدم `0.0.0.0:8000` وليس `localhost:8000` للسماح بالاتصالات الخارجية

### 2.2 تشغيل ngrok

// turbo
في PowerShell جديد (ثاني), نفذ:
```powershell
C:\ngrok\ngrok.exe http 8000
```

أو إذا كان ngrok في المسار (PATH):
```powershell
ngrok http 8000
```

---

## الخطوة 3: الحصول على الرابط العام

بعد تشغيل ngrok، ستظهر شاشة مشابهة لهذه:

```
ngrok

Session Status                online
Account                       your_email@example.com (Plan: Free)
Version                       3.x.x
Region                        United States (us)
Latency                       -
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://abcd-1234-5678.ngrok-free.app -> http://localhost:8000

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

### الرابط العام هو

انظر إلى السطر `Forwarding`، الرابط الذي يبدأ بـ `https://` هو رابطك العام.

مثال:
```
https://abcd-1234-5678.ngrok-free.app
```

---

## الخطوة 4: مشاركة الرابط

الآن يمكنك:
1. ✅ نسخ الرابط وإرساله لأي شخص
2. ✅ فتحه على هاتفك من أي مكان
3. ✅ الوصول إليه من أي جهاز حاسوب آخر
4. ✅ العمل عليه حتى لو لم تكن على نفس الشبكة

---

## نصائح مهمة

### ⚠️ تحذيرات الأمان

1. **لا تشارك الرابط مع أشخاص غير موثوقين** - أي شخص لديه الرابط يمكنه الوصول للتطبيق
2. **استخدم اسم مستخدم وكلمة مرور قوية** للتطبيق
3. **أغلق ngrok عندما لا تحتاجه** (اضغط Ctrl+C في نافذة ngrok)

### 🔄 في كل مرة تشغل ngrok

- الرابط يتغير في كل مرة تشغل فيها ngrok (في النسخة المجانية)
- إذا أردت رابط ثابت، يجب الترقية لخطة مدفوعة من ngrok

### 📱 استخدام الرابط على الهاتف

1. افتح المتصفح على هاتفك (Chrome أو Safari)
2. الصق الرابط
3. قد تظهر صفحة تحذير من ngrok، اضغط "Visit Site"
4. سجل دخول واستخدم التطبيق بشكل طبيعي

### 🖥️ لوحة تحكم ngrok

يمكنك مراقبة جميع الطلبات من خلال:
```
http://127.0.0.1:4040
```
افتح هذا الرابط في متصفحك أثناء تشغيل ngrok لرؤية جميع الطلبات الواردة.

---

## استكشاف الأخطاء

### ❌ المشكلة: "This site can't be reached"

**الحل:**
- تأكد من أن Django يعمل على `0.0.0.0:8000`
- تأكد من أن ngrok يشير إلى المنفذ 8000

### ❌ المشكلة: "Invalid Host header"

**الحل:**
✅ تم تحديث `settings.py` تلقائياً ليقبل جميع المضيفين

### ❌ المشكلة: "CSRF verification failed"

**الحل:**
✅ تم تحديث إعدادات CSRF في `settings.py` لدعم ngrok

---

## الإيقاف

لإيقاف كل شيء:

1. **إيقاف ngrok**: في نافذة ngrok، اضغط **Ctrl+C**
2. **إيقاف Django**: في نافذة Django، اضغط **Ctrl+C**

---

## خيارات متقدمة (اختيارية)

### استخدام نطاق مخصص (Custom Domain)

إذا كان لديك اشتراك مدفوع في ngrok:
```powershell
ngrok http 8000 --domain=your-domain.ngrok.io
```

### حماية الرابط بكلمة مرور

```powershell
ngrok http 8000 --basic-auth="username:password"
```

### تحديد المنطقة الجغرافية

للحصول على أداء أفضل:
```powershell
ngrok http 8000 --region=eu  # أوروبا
ngrok http 8000 --region=ap  # آسيا
ngrok http 8000 --region=us  # أمريكا
```

---

## ملخص سريع

```powershell
# Terminal 1: تشغيل Django
cd C:\Users\2025\.gemini\antigravity\scratch\electoral_office
python manage.py runserver 0.0.0.0:8000

# Terminal 2: تشغيل ngrok
C:\ngrok\ngrok.exe http 8000

# انسخ الرابط من Forwarding وشاركه!
```

---

**ملاحظة:** هذا الحل مناسب للتطوير والاختبار. للإنتاج الفعلي، يُنصح باستخدام استضافة سحابية احترافية.
