# ✅ Railway CLI جاهز! - الخطوات التالية

## 🎉 ما تم إنجازه:

✅ Node.js موجود (v24.12.0)  
✅ PowerShell تم تهيئته  
✅ Railway CLI تم تثبيته (v4.25.1)

---

## 🚀 الخطوات التالية (يدوياً)

افتح **PowerShell جديد** (مهم!) واتبع هذه الخطوات:

---

### الخطوة 1: الانتقال لمجلد المشروع

```powershell
cd C:\Users\2025\.gemini\antigravity\scratch\electoral_office
```

---

### الخطوة 2: تسجيل الدخول

```powershell
railway login
```

✨ **سيفتح متصفح تلقائياً** لتسجيل الدخول. اضغط **"Authorize"** ثم ارجع للـ PowerShell.

ستظهر رسالة: `✅ Logged in as [your-email]`

---

### الخطوة 3: ربط المشروع

```powershell
railway link
```

✨ ستظهر قائمة بمشاريعك. اختر:
- **valiant-presence** (أو electoral-office)

استخدم الأسهم ⬆️⬇️ للتنقل و Enter للاختيار

ستظهر رسالة: `✅ Linked to project: valiant-presence`

---

### الخطوة 4: الاتصال بقاعدة البيانات

```powershell
railway connect postgres
```

أو إذا لم ينجح، جرب:

```powershell
railway connect Postgres-ky75
```

✨ ستظهر واجهة PostgreSQL:
```
psql (xx.x)
Type "help" for help.

postgres=#
```

---

### الخطوة 5: تنفيذ أوامر SQL

الآن **انسخ والصق كل سطر على حدة** ثم اضغط Enter:

#### السطر 1:
```sql
ALTER TABLE elections_voter ALTER COLUMN phone TYPE VARCHAR(30);
```

يجب أن تظهر: `ALTER TABLE`

#### السطر 2:
```sql
ALTER TABLE elections_voter DROP CONSTRAINT IF EXISTS elections_voter_phone_key;
```

يجب أن تظهر: `ALTER TABLE` أو `NOTICE: constraint "elections_voter_phone_key" ... does not exist`

#### السطر 3:
```sql
ALTER TABLE elections_voter ALTER COLUMN phone DROP NOT NULL;
```

يجب أن تظهر: `ALTER TABLE`

---

### الخطوة 6: الخروج

```sql
\q
```

✅ **تم!** الآن قاعدة البيانات جاهزة

---

## 🔄 الخطوة النهائية: إعادة استيراد البيانات

افتح هذا الرابط في المتصفح:

```
https://web-production-42c39.up.railway.app/tool/import-final-data/?secret=shems_voter_import_2024_secure
```

⏱️ **انتظر 15-20 دقيقة**

---

## 🔍 التحقق من النجاح

### 1. تحقق من العدد الكلي:
```
https://web-production-42c39.up.railway.app/dashboard/
```
يجب أن يصبح: **~1,868,933 ناخب**

### 2. ابحث عن رقمك:
```
https://web-production-42c39.up.railway.app/voter-search/
```
أدخل: **33037821** ✅

---

## 🆘 إذا واجهت مشاكل

### مشكلة: "railway: command not found"
أغلق PowerShell وافتحه من جديد

### مشكلة: "No service named postgres found"
نفّذ:
```powershell
railway services
```
سيظهر لك اسم الخدمة الصحيح

### مشكلة: انقطع الاتصال أثناء SQL
أعد الاتصال:
```powershell
railway connect postgres
```
ونفّذ الأوامر المتبقية

---

## 📝 ملخص الأوامر (نسخة سريعة)

```powershell
# 1. الانتقال للمشروع
cd C:\Users\2025\.gemini\antigravity\scratch\electoral_office

# 2. تسجيل دخول
railway login

# 3. ربط المشروع
railway link

# 4. الاتصال بقاعدة البيانات
railway connect postgres

# 5. تنفيذ SQL (واحد تلو الآخر)
ALTER TABLE elections_voter ALTER COLUMN phone TYPE VARCHAR(30);
ALTER TABLE elections_voter DROP CONSTRAINT IF EXISTS elections_voter_phone_key;
ALTER TABLE elections_voter ALTER COLUMN phone DROP NOT NULL;

# 6. خروج
\q
```

---

**الحالة:** ✅ CLI جاهز - ابدأ من الخطوة 1 في PowerShell جديد  
**التاريخ:** 16 يناير 2026 - 00:30
