# 📊 استيراد 1.9 مليون ناخب إلى Railway

## الوضع الحالي
- ✅ الموقع يعمل على https://web-production-42c39.up.railway.app
- ❌ قاعدة البيانات فارغة (لا يوجد ناخبون)
- 📦 قاعدة البيانات المحلية تحتوي على: **1,868,933 ناخب**

---

## 🚀 الخطوات (الطريقة الموصى بها)

### 1️⃣ تصدير البيانات على دفعات

```powershell
cd C:\Users\2025\.gemini\antigravity\scratch\electoral_office
python export_voters_batches.py
```

**المدة المتوقعة**: 15-30 دقيقة  
**النتيجة**: مجلد `voter_batches` يحتوي على ~38 ملف JSON

### 2️⃣ ضغط الملفات

```powershell
Compress-Archive -Path voter_batches -DestinationPath voter_batches.zip
```

**الحجم المتوقع**: ~200-400 MB

### 3️⃣ رفع للمستودع

```powershell
# قد يكون الملف كبيراً لـ Git
# استخدم Git LFS أو خدمة تخزين سحابي

# إذا كان أقل من 100 MB:
git add voter_batches/*.json
git commit -m "إضافة بيانات الناخبين (دفعات)"
git push origin main

# أو استخدم Railway CLI لرفع مباشر:
railway up voter_batches:/app/voter_batches
```

### 4️⃣ الاستيراد على Railway

```powershell
# ثبّت Railway CLI إذا لم تفعل:
iwr https://railway.app/install.ps1 -useb | iex

# سجل دخول وربط المشروع:
railway login
railway link  # اختر: valiant-presence

# شغّل الاستيراد:
railway run python import_voters_batches.py
```

**المدة المتوقعة**: 30-60 دقيقة  
**سيعرض التقدم مباشرة**

---

## ⚡ طريقة بديلة (أسرع - للمحترفين)

### استخدام pg_dump مباشرة

```powershell
# 1. حوّل SQLite إلى PostgreSQL محلياً
# ثبّت pgloader: https://pgloader.io/
pgloader db.sqlite3 postgresql://localhost/electoral_temp

# 2. صدّر من PostgreSQL
pg_dump -Fc electoral_temp > electoral.dump

# 3. احصل على DATABASE_URL من Railway
railway variables | findstr DATABASE_URL

# 4. استورد مباشرة
pg_restore -d "postgresql://..." electoral.dump
```

**المدة المتوقعة**: 10-15 دقيقة  
**الأسرع لكن يحتاج أدوات إضافية**

---

## 📝 إحصائيات البيانات

| البيان | العدد |
|--------|-------|
| الناخبون | 1,868,933 |
| المرشحون | 0 |
| المراسي | 0 |
| المعرفون | 0 |
| **حجم قاعدة البيانات** | **1.1 GB** |

---

## ✅ التحقق بعد الاستيراد

```powershell
# على Railway:
railway run python manage.py shell

# في shell:
from elections.models import Voter
print(f"عدد الناخبين: {Voter.objects.count():,}")
# يجب أن يظهر: 1,868,933
```

أو زر الموقع:
1. افتح https://web-production-42c39.up.railway.app
2. سجل دخول بـ admin / admin123
3. اذهب لصفحة الناخبين
4. يجب أن ترى القائمة مليئة بالبيانات

---

## 🐛 حل المشاكل

### المشكلة: "Out of memory"
**الحل**: استخدم دفعات أصغر (غيّر BATCH_SIZE إلى 25000)

### المشكلة: "Connection timeout"
**الحل**: زد timeout على Railway:
```python
# في settings_production.py:
DATABASES['default']['CONN_MAX_AGE'] = 600
```

### المشكلة: "File too large for git"
**الحل**: استخدم railway up مباشرة أو خدمة تخزين سحابي

---

## 💡 نصائح مهمة

1. 🕐 **الوقت**: العملية تأخذ وقت - لا تقطعها
2. 💾 **النسخ الاحتياطي**: احتفظ بنسخة من db.sqlite3
3. 🔄 **يمكن تكرار الاستيراد**: إذا فشل، شغّله مرة أخرى
4. 📊 **راقب Railway Metrics**: تابع استخدام الذاكرة والـ CPU

---

## 🎯 السكريبتات المتوفرة

| السكريبت | الاستخدام |
|----------|-----------|
| `export_voters_batches.py` | تصدير محلي على دفعات |
| `import_voters_batches.py` | استيراد على Railway |
| `migrate_large_database_guide.py` | دليل الطرق الأخرى |

---

**آخر تحديث**: 2026-01-12 06:07  
**عدد الناخبين**: 1,868,933  
**الموقع**: https://web-production-42c39.up.railway.app
